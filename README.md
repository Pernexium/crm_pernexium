# CRM_Pernexium

Correr aplicación
```bash
streamlit run <path_to_script>/app.py
```

## Secrets

En `.streamlit/secrets.toml` con la estructura

```bash
[db_credentials]
user = "user"
password = "password"
host = "host"
database = "database"
port = "port"
```